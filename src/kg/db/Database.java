package kg.db;

import kg.models.Car;
import kg.models.Person;
import kg.models.SocialMedia;

import java.util.LinkedList;
import java.util.List;

public class Database {

    public static List<Person> persons = new LinkedList<>();
    public static List<Car> cars = new LinkedList<>();
    public static List<SocialMedia> socialMedias = new LinkedList<>();
}
